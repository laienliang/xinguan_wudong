# 测试报告

## 1. 测试概况

- 测试日期：2026年9月10日
- 项目：cool-admin-midway
- 测试类型：全量自动化测试、造数校验、构建验证
- 测试数据库：`cool_test`
- 业务数据核验数据库：`cool`
- 测试标记：`[TEST_SEED_20260909]`

## 2. 测试环境

- Node.js：按项目 `package.json` 要求使用 Node.js 18 及以上
- 数据库：MySQL `127.0.0.1:3306`
- 测试服务端口：自动分配临时端口
- 本地后端端口 `8001`：测试结束后已关闭
- 管理前端端口 `9000`：测试结束后已关闭

## 3. 测试数据

已执行 `scripts/seed-test-data.js`，向 `cool` 和 `cool_test` 各写入一套测试数据，覆盖：

- 非遗商品、商品 SKU、商品图片
- 餐厅、菜品、农产品、预约时段
- 民宿、房型、房态日历
- 景区、门票、旅游线路、线路行程
- 社区话题、游记、评论、举报
- 平台公告、平台轮播
- 商家账号、商家入驻申请

脚本支持重复执行，仅清理带测试标记的数据，不影响其他业务数据。

## 4. 测试用例

新增测试文件：`test/integration/seeded-module-data.test.ts`

覆盖内容：

1. 校验商品、餐饮、住宿、旅游、社区、平台公告造数记录存在。
2. 校验六个新增管理模块菜单存在。
3. 校验管理菜单权限节点数量不少于 30 个。

## 5. 测试结果

执行命令：

```bash
npm test -- --runInBand
```

结果：

- 测试套件：23 个通过，23 个总计
- 测试用例：205 个通过，205 个总计
- 新增造数集成用例：7 个通过，7 个总计
- 失败用例：0

## 6. 构建验证

执行命令：

```bash
npm run build
```

结果：构建成功，实体文件生成和 TypeScript 编译均通过。

## 7. 通过率与覆盖率

- 测试套件通过率：`23/23 = 100%`
- 测试用例通过率：`205/205 = 100%`
- 测试用例失败率：`0/205 = 0%`
- 语句覆盖率：`52.16%`（2073/3974）
- 行覆盖率：`47.67%`（1710/3587）
- 函数覆盖率：`11.84%`（59/498）
- 分支覆盖率：`6.59%`（55/834）

覆盖率采集已修正。此前配置使用了 `*.service.ts`、`*.controller.ts` 文件名匹配，但项目实际采用 `service/`、`controller/` 目录组织源码，导致匹配不到任何文件，才显示为 0%。

本次修正为按目录采集：`src/modules/**/service/**/*.ts` 和 `src/modules/**/controller/**/*.ts`。测试用例本身仍为 205/205 通过；由于现有全局覆盖率阈值高于当前实际覆盖率，带 `--coverage` 的命令会因阈值未达标返回失败，但覆盖率数据已正常生成。

## 8. 结论

本次造数、菜单权限和全量自动化测试均通过，未发现阻断性问题。测试完成后后端和管理前端保持关闭状态。
